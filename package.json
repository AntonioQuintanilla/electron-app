{
  "name": "my-electron-app",
  "version": "1.0.0",
  "description": "First electron app",
  "main": "src/index.js",
  "scripts": {
    "start": "electron .",
    "clean-start": "npm cache clean --force && rm -rf node_modules package-lock.json && npm install && npm start",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "electron": "^32.1.0"
  },
  "dependencies": {
    "promise-mysql": "^5.2.0",
    "uuid": "^10.0.0"
  }
}
